# Base URL of the API. Leave unset in development — the Vite dev server
# proxies /api to the backend (see vite.config.js). Set for production
# builds, e.g. https://api.your-hospital.com/api
VITE_API_URL=
